const color = {
	pm: '#74EBD5',
	sd: '#9FACE6'
}
const border = {
	sm: '1px solid rgba(0, 0, 0, 0.05)',
	md: '2px solid rgba(0, 0, 0, 0.05)'
}

const gradient = {
	pm: `linear-gradient(to top left, ${color.pm}, ${color.sd})`
}

const theme = {
	color,
	border,
	gradient
}

export default theme
